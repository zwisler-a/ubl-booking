import { AreaSanitationPipe } from './area-sanitation.pipe';

describe('AreaSanitationPipe', () => {
  it('create an instance', () => {
    const pipe = new AreaSanitationPipe();
    expect(pipe).toBeTruthy();
  });
});
