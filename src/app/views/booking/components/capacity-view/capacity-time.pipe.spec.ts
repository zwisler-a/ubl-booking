import { CapacityTimePipe } from './capacity-time.pipe';

describe('CapacityTimePipe', () => {
  it('create an instance', () => {
    const pipe = new CapacityTimePipe();
    expect(pipe).toBeTruthy();
  });
});
