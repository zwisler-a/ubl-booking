import { TimeslotIntervalPipe } from './timeslot-interval.pipe';

describe('TimeslotIntervalPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeslotIntervalPipe();
    expect(pipe).toBeTruthy();
  });
});
